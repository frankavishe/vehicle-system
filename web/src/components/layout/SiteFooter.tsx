export function SiteFooter() {
  return (
    <footer className="mt-auto border-t border-line">
      <div className="mx-auto max-w-6xl px-4 py-8 text-xs text-steel-soft sm:px-6">
        AutoServe — genuine and compatible spare parts, mechanic dispatch and
        recovery, across Tanzania.
      </div>
    </footer>
  );
}
